This folder contains a sample java folder. To build this project follow the commands below:

cd  app/
mvn clean
mvn install
mvn exec:java -Dexec.mainClass="com.hajira.App"

The output will be a Hello World.

